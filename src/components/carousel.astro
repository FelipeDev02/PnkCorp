<div id="mainCarousel" class="carousel slide custom-carousel shadow-lg" data-bs-ride="carousel">
    <div class="carousel-inner rounded-4 border border-white border-opacity-10">
        <div class="carousel-item active">
            <img src="https://picsum.photos/id/10/1200/500" class="d-block w-100" alt="...">
            <div class="carousel-overlay">
                <h3>Sistemas Cloud AWS</h3>
                <p>Infraestructura escalable para PnK Corp.</p>
            </div>
        </div>
        <div class="carousel-item">
            <img src="https://picsum.photos/id/20/1200/500" class="d-block w-100" alt="...">
            <div class="carousel-overlay">
                <h3>Base de Datos Postgres</h3>
                <p>Seguridad y rendimiento en cada consulta.</p>
            </div>
        </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
    </button>
</div>

<style>
    .custom-carousel {
        max-width: 900px;
        margin: 0 auto;
    }
    .carousel-item img {
        height: 450px;
        object-fit: cover;
        filter: brightness(0.6);
    }
    .carousel-overlay {
        position: absolute;
        bottom: 40px;
        left: 40px;
        z-index: 10;
        font-family: 'Syne', sans-serif;
    }
    .carousel-overlay h3 { font-weight: 800; color: #7b8bff; }

    @media (max-width: 768px) {
        .carousel-item img { height: 300px; }
    }
</style>